# DESIGN.md — HR Helper

## 产品与画像
- **产品**：HR Helper —— HR 的「数字员工」工作台。左侧导航，右侧主区；点击员工卡片即进入与该岗位 AI 的对话。
- **用户画像**：企业 HR / HRBP / 人事专员，在办公桌前高频使用，需要克制、专业、可信赖的工具感。

## 气质与意象
- **意象锚点**：清晨的现代企业开放办公区——白色桌面、浅灰地毯、柔和的北向日光透过百叶窗；像 Notion / Linear 那种克制的 SaaS 质感，但保留"人味儿"——数字员工是拟人化同事，头像是友好的扁平插画。
- 关键词：克制、专业、温和、有序、留白。

## Design Tokens

### 色彩
- 页面背景：`#F4F6F9`（冷调灰白，像办公区地毯）
- 侧边栏：`#FFFFFF`，右侧 1px `#E9ECF1` 分隔
- 卡片底色：`#FFFFFF`
- 主文本：`#1F2937`；次文本：`#6B7280`；占位/弱提示：`#9CA3AF`
- 深色强调（导航激活胶囊 / 主按钮）：`#111827`
- WORK 标签：薄荷绿底 `#D1FAE5` + 字 `#059669`
- BOSS 标签：琥珀金底 `#FEF3C7` + 字 `#B45309`
- 用户气泡：`#111827` 白字；AI 气泡：`#FFFFFF` + 1px `#E9ECF1` 边框
- 成功对勾：`#10B981`

### 字体
- 中文：`"PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans SC", sans-serif`
- 英文/数字：`Inter`（Google Fonts，CN 域名），回退系统无衬线
- 字重：页面/卡片标题 700，正文 400，标签 600

### 圆角与阴影
- 卡片圆角：`16px`；按钮/输入框/标签：`10px`；头像：圆形
- 卡片阴影：`0 1px 2px rgba(17,24,39,.04), 0 1px 3px rgba(17,24,39,.06)`
- Hover 阴影：`0 8px 24px rgba(17,24,39,.08)`
- 禁用大圆角（>20px）、重投影

### 间距
- 侧边栏宽 `248px`；内容区 padding `32px 40px`
- 卡片网格 gap `20px`；卡片内 padding `24px`

## 布局与响应式
- 左固定侧边栏 + 右滚动内容区
- 员工卡片网格：桌面 4 列（≥1280px），中屏 3 列，平板 2 列，手机 1 列
- 对话页：顶部员工信息条（可返回）+ 消息流 + 底部输入区

## 组件规范
- 员工卡片：顶部左侧 WORK/BOSS 标签 → 居中头像（64px 圆）→ 岗位名（18px 700）→ 职责描述（13px 灰，2 行截断）→ 底部"已完成 N 任务"（12px 绿对勾）
- 导航项：图标 + 文字，激活项为深色胶囊（白字），hover 浅灰底
- 消息气泡：用户右对齐深色气泡；AI 左对齐白底气泡 + 头像；流式输出带闪烁光标
- 输入区：圆角输入框 + 圆形发送按钮（深色）

## 动效与交互
- 卡片 hover：`translateY(-2px)` + 阴影加深，`160ms ease-out`
- 路由切换：淡入 + 4px 上移，`200ms`
- 对话流式：打字机逐字追加，末尾 `▍` 光标闪烁
- 缓动统一 `cubic-bezier(0.22, 1, 0.36, 1)`（easeOutQuint）

## 设计禁忌
- 不要蓝紫渐变、霓虹光、赛博风
- 不要花哨大圆角（>20px）与超重投影
- 不要表情符号充数；角色一律用扁平插画头像
- 不要密集排版；保持 SaaS 留白节奏
